# Reaction Rush v2 — runtime dependencies
#
# The GAME ITSELF (server + client) uses ONLY the Python standard library:
#   socket, threading, queue, time, json, random, argparse, sqlite3,
#   tkinter, dataclasses, typing, logging, uuid, statistics
#
# No third-party packages are required to run the server or the client.
#
# On Linux, Tkinter may need to be installed separately:
#   sudo apt install python3-tk
#
# ---------------------------------------------------------------------------
# Development / testing only (NOT needed to play):
#   pip install -r requirements-dev.txt

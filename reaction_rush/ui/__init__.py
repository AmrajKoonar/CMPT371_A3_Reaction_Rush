"""
reaction_rush.ui — Reusable Tkinter UI layer.

Split into small modules so no single file carries all the UI code:

    theme.py       Colour system + dark/light palettes and fonts.
    components.py  Reusable styled widgets (buttons, cards, entries, ...).
    sounds.py      Optional cross-platform sound feedback.
    screens.py     Builders for each application screen.
"""
